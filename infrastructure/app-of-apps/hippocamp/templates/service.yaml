apiVersion: v1
kind: Service
metadata:
  name: hippocamp-service
spec:
  selector:
    app: hippocamp
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.port }}
  type: LoadBalancer